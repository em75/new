name: Example Workflow with Email in Step Name

on:
  workflow_dispatch:  # Allows you to manually trigger the workflow

jobs:
  test:
    runs-on: ubuntu-latest  # Use Ubuntu runner for this job

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Step with your email address in the name
      - name: 22f2000918@ds.study.iitm.ac.in  # Email in the step name
        run: echo "Hello, world!"  # This step runs a simple echo command
